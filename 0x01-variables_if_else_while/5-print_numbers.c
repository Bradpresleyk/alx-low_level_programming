#include <stdio.h>
/**
 * main - Function that outputs single digits 0-9
 * Return: 0 (Success)
 */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	print("\n");
	return (0);
}
