#include <stdio.h>
/**
 * main - Function that prints the alphabet in lowercase
 * Return: 0 (Success)
 */
int main(void)
{
	int n = 96;
	while(n != 123)
	{
		putchar(n + 1);
		n++;
	}
	putchar("\n");
	return (0);
}
